swagger: "2.0"
definitions:
  userAccessKey:
    x-jdcloud-module: jmr
    title: userAccessKey
    type: object
    properties:
      id:
        type: integer
        description: "User Pass ID"
      pin:
        type: string
        description: "User Name"
      account:
        type: string
        description: "User Account"
      accessKey:
        type: string
        description: "Access key, AccessKey is used for calling cloud service API with program method"
      accessKeySecret:
        type: string
        description: "AccessKeySecret is the key pair used to verify the user"
      expire:
        type: string
        format: date
        description: "Expiration Time"
      created:
        type: string
        format: date-time
        description: "Creation Time"
      modified:
        type: string
        format: date-time
        description: "Update Time"
      modifier:
        type: string
        description: "Update Operator"
      state:
        type: integer
        description: "Status"
      yn:
        type: integer
