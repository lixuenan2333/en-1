swagger: "2.0"
definitions:
  selectParams:
    x-jdcloud-module: jmr
    title: selectParams
    type: object
    properties:
      pageNum:
        type: integer
        description: "Search Paging Number, optional condition"
      pageSize:
        type: integer
        description: "Search Paging Size, optional condition"
      orderBy:
        type: string
        description: "Ranking Condition, optional"
      status:
        type: string