swagger: "2.0"
definitions:
  quota:
    x-jdcloud-module: common
    type: object
    properties:
      name:
        type: string
        description: "Name of the Quota Item"
      max:
        type: integer
        description: "Quota"
      used:
        type: integer
        description: "Number Used"