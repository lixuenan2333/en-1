swagger: "2.0"
definitions:
  target:
    x-jdcloud-module: streambus
    title: target
    type: object
    properties:
      name:
        type: string
        description: "Destination of Archiving"
      recordSize: 
        type: integer
        description: "It starts to archive when achieving the data bulk"
      cycle:
        type: integer
        description: "Time Period of Performing Archiving Task"